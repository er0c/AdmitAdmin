/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

// next.config.js

module.exports = {
    images: {
      domains: ['admitbackend.online'], // Add this line
    },
    // Other configurations...
  };
  